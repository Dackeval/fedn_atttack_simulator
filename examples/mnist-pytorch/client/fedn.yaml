# only run build and startup through fedn.yaml. Train and Validate through docker_client.py
python_env: python_env.yaml 

entry_points:
  build:
    command: python model.py
  startup:
    command: python data.py



